/** No. of elements are fiexed */
/** type of elements are know and in  same orders */
var skill = ['JS', 9];
console.log(skill);
console.log(skill[0]);
console.log(skill[1]);
console.log(skill.length);
// Tuples with optional elements (?)
var color = [255, 255, 0, 0.4];
console.log("Current Color is ", color);
